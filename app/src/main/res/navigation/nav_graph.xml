<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/listOfTracksFragment">
    <fragment
        android:id="@+id/listOfTracksFragment"
        android:name="kpfu.itis.valisheva.app1.fragments.ListOfTracksFragment"
        android:label="ListOfTracksFragment"
        tools:layout="@layout/fragment_list_of_tracks">
        <action
            android:id="@+id/action_listOfTracksFragment_to_trackFragment"
            app:destination="@id/trackFragment" />
    </fragment>
    <fragment
        android:id="@+id/trackFragment"
        android:name="kpfu.itis.valisheva.app1.fragments.TrackFragment"
        android:label="TrackFragment"
        tools:layout="@layout/fragment_track">

        <action
            android:id="@+id/action_trackFragment_to_listOfTracksFragment"
            app:destination="@id/listOfTracksFragment" />
    </fragment>

</navigation>